services:
  web:
    build: .
    ports:
      - "5000:5000"
    env_file: .env
    environment:
      - FLASK_ENV=development
      - VIRTUAL_ENV=/app/venv
    # Sometimes needed for DNS issues in corporate/restricted networks
    dns:
      - 8.8.8.8
      - 8.8.4.4
